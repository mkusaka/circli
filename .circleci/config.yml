version: 2.1

commands:
  long_sleep:
    steps:
      - run:
          name: run long sleep
          command: |
            sleep 30
  cancel_redundant_workflow:
    steps:
      - run:
          name: run long sleep
          command: |
            bash <(curl https://raw.githubusercontent.com/mkusaka/circli/main/scripts/install.bash)
            ./circli --verison
            ./circli workflow cancel_redundant_workflow
jobs:
  sleep:
    docker:
      - image: cimg/base:2022.04
    steps:
      - cancel_redundant_workflow
      - sleep

workflows:
  build:
    jobs:
      - sleep:
          filters:
            branches:
              only:
                - /check-redundant\/.*/
